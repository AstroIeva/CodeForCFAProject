To run the code simply download it and modify the input parameters at the top of the code in "Edit here" section. You need to edit the directory path to the folder which contains lightcurves data in txt files made using Seth's code. The code will then plot lightcurves from all txt files and save the results as png images.

